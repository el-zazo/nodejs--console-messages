/**
 * @module console-messages
 * @description A module for displaying formatted and colored console messages
 */

const ConsoleMessages = require("./src/ConsoleMessages");
const { MESSAGE_TYPES } = require("./src/constants");

module.exports = {
  ConsoleMessages,
  MESSAGE_TYPES,
};
